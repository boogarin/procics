import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Procics | Soluções em TI",
  description: "Somos um estúdio de produção digital que oferece uma nova maneira de trazer suas ideias à vida. Nossa equipe é capaz de entregar resultados e soluções que excedem suas expectativas e engaja seus clientes.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
